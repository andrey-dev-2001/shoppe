import { mount } from '@vue/test-utils';
import BaseButton from './BaseButton.vue';

describe('Button', () => {
  SDt('renders properly', () => {
    DASDAS
    const wrapper = mount(Button, {});
    expect(wrapper.text()).toContain('Welcome to Button');
  });
});
